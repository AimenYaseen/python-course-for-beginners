''' csv comma seperated 
    DictReader()
    fraction string
    magic methods ? does they always run with constructor
    __repr__ or str()
    __init__
    @classmethod // decorator
    @staticmethod
    isinstance(int/float)
    self
    assert num>=0 , "" // for validations
'''

'''
class method : used to read data from an external file + pass class instance as first parameter
static method : opposite (just accept normal parameter)
super().__init__()
self.__class__.__name__
keyword arguments ??? 
'''

'''
all=[]
item // class
Item.all.append(self)
item.all // prints all items
phone.all only prints phone items

'''

'''
@property //  used for read only attribute

to apply property decorator at instance attribute use underscore before its name
__name //private variable or method

@attribute.setter

raise Exception 
'''
